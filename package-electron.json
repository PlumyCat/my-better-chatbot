{
  "name": "better-chatbot-desktop",
  "version": "1.21.0",
  "description": "Better Chatbot Desktop Application",
  "main": "electron/main.js",
  "author": "cgoinglove",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "build:next": "next build && next export -o out",
    "build:next:standalone": "cross-env NO_HTTPS=1 next build",
    "build:electron": "electron-builder",
    "build:win": "npm run build:next:standalone && npm run build:electron -- --win",
    "build:win32": "npm run build:next:standalone && npm run build:electron -- --win --ia32",
    "build:win64": "npm run build:next:standalone && npm run build:electron -- --win --x64",
    "build:mac": "npm run build:next:standalone && npm run build:electron -- --mac",
    "build:linux": "npm run build:next:standalone && npm run build:electron -- --linux",
    "dist": "npm run build:next:standalone && electron-builder",
    "postinstall": "electron-builder install-app-deps"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0",
    "electron-serve": "^1.3.0"
  },
  "devDependencies": {
    "electron": "^33.3.1",
    "electron-builder": "^25.1.8",
    "cross-env": "^7.0.3"
  }
}
